# Create TeqFW Conventions AGENTS

## Goal

Create `ctx/docs/code/conventions/teqfw/AGENTS.md` for the new documentation level after analyzing the `AGENTS.md` hierarchy from the project root down to the target directory.

## Performed Actions

- Reviewed the active instruction hierarchy in `AGENTS.md`, `ctx/AGENTS.md`, `ctx/docs/AGENTS.md`, and `ctx/docs/code/AGENTS.md`.
- Verified the current contents of `ctx/docs/code/conventions/teqfw/` to build an accurate `Level Map` for the new level.
- Inspected the historical version of `ctx/docs/code/conventions/AGENTS.md` from `HEAD` to preserve the established wording style while working in a tree where that file is currently deleted.
- Added `ctx/docs/code/conventions/teqfw/AGENTS.md` with `Purpose`, `Level Map`, `Level Boundary`, and `Summary` sections.

## Produced Artifacts

- `ctx/docs/code/conventions/teqfw/AGENTS.md` — navigation and boundary document for the TeqFW-specific conventions level.

## Outcome

The new `teqfw/` documentation level now has its own local `AGENTS.md` with a valid `Level Map` and scope description aligned with the surrounding ADSM documentation hierarchy.
